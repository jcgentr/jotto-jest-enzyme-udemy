import React from "react";

const RandomWordError = () => {
	return (
		<div className='alert alert-danger' role='alert'>
			Could not fetch random word!
		</div>
	);
};

export default RandomWordError;
